# Cardápio RedBurger

Projeto Django para exibir cardápios de lanchonetes e seus produtos.

## Como executar

```powershell
venv\Scripts\Activate.ps1
pip install -r requirements.txt
python manage.py migrate
python manage.py runserver
```

Acesse `http://127.0.0.1:8000/`.
